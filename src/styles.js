import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  main: {
    textAlign: 'center',
  },
  root: {
    backgroundColor: "red",
    color: "white",
  },
});

export default useStyles;
